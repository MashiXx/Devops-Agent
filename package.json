{
  "name": "devops-agent",
  "version": "2.0.0",
  "description": "AI DevOps Agent — Claude + Real SSH",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && node server.js\" \"cd frontend && npm start\" --names \"backend,frontend\" --prefix-colors \"cyan,green\"",
    "start:backend": "cd backend && node server.js",
    "start:frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
